import React, { Component } from 'react';

export default class Monitor extends Component {
  render() {
    return (
      <div>
        <p>Monitor</p>
        <p>
          This is why understanding Route was so important. Nothing has changed from earlier to right now, but for some
          reason, your brain gets all worked up since we’re rendering a Route outside of our main App component
        </p>
      </div>
    );
  }
}
